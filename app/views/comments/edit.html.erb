<h1>Editing Comment</h1>

<%= render 'form', comment: @comment %>

<%= button_to 'Show', @comment, :method => :get, class: "btn btn-secondary button-float" %>
<%= button_to 'Back', report_path(@comment.report_id), :method => :get, class: "btn btn-secondary button-float" %>
